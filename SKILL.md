---
name: ouyezi
description: >
  通用代码性能优化方法论技能。基于鲲鹏技术文档提炼的跨平台、架构无关的优化原则：
  热点驱动的工作流（基线测量→剖析定位→优化→复测验证）、流水线编排与延迟隐藏、
  缓存友好性设计（数据布局、访问模式、伪共享、预取）、SIMD 向量化思想、循环变换、
  并行分解与 NUMA 意识、编译器优化（优化等级/内联/LTO/PGO）、数值精度与确定性权衡。
  触发词：性能优化、调优、性能瓶颈、热点分析、profile、剖析、缓存优化、伪共享、
  向量化、SIMD、循环优化、并行优化、吞吐优化、延迟优化、精度优化、性能基线、优化复测。
  Do NOT trigger for: 绑定特定硬件指令集（NEON/SVE/AVX 等）的专属优化、厂商专有
  工具或 SDK 的配置、不含性能诉求的普通功能开发、安全审查、依赖分析。
license: MIT
---

# 通用代码性能优化方法论

> 跨平台、架构无关的性能优化最佳实践指南。原则提炼自鲲鹏系列技术文档
> （GPU 应用优化白皮书、性能优化十板斧、精度优化三板斧、编程与调优指南、计算精度白皮书），
> 但刻意去除所有芯片/厂商专属细节，只保留在任何硬件平台上成立的方法论。

## Setup

无外部依赖。执行时直接按下面的 Process 走：

- 需要完整原则目录时 Read `references/methodology.md`
- 需要分步操作细节（21 步）时 Read 本目录下 **`kp-guide.md`**（Process 各步标注的"指南 Steps N–M"即指此文件的对应 Step）
- 需要数值精度细节时 Read `references/methodology.md` 的 **§11 浮点精度与数值稳定** 与 **§12 精度-性能权衡与确定性** 两节

本技能不需要读取 `kp/` 源语料（PDF/图片）即可工作。

## Process

1. **确认场景与瓶颈类型**（指南 Steps 1–2）：先判断问题是计算密集、IO 密集还是网络密集，
   明确"性能"在本业务里指什么（吞吐/延迟/功耗），并确立基线测量与优化目标。
2. **剖析定位热点**（指南 Steps 3–4）：用采样剖析找出时间占比最高的热点路径，
   从系统视图下钻到单个热点函数；低利用率时用系统调用追踪、调度延迟分析、线程落点观测
   找阻塞点；先修复跨切面的系统性问题，再钻入最热点。
3. **选择并应用优化手段**（指南 Steps 5–15）：按热点类型从缓存友好、SIMD 向量化、
   循环变换、并行分解、NUMA/亲和性、CPU 流水线认知、流水线编排、编译器优化、
   IO 与算法替代中选取对应手段，每次只优化一个热点并保持可回滚。
4. **复测验证与量化收益**（指南 Steps 16–17）：同一测试复测，用
   `(基线-优化后)/基线` 记录改善比例；验证命中预期瓶颈，负面优化立即回滚。
5. **数值精度与确定性检查**（指南 Steps 18–19）：涉及浮点/并行归约/跨平台复现时，
   评估误差指标（RMSE/MAE/相关系数）与 IEEE 754 基础，确认精度-性能权衡符合业务容差；
   混合精度作风险源核查，配置纪律钉死复现变量。
6. **运行时调优与归档**（指南 Steps 20–21）：带 GC 的托管运行时量化 GC 频率/停顿后调优；
   记录环境、参数、过程数据与最终收益，形成可追溯、可复现的优化记录。

## Mode line

默认全流程。若用户只要单一能力（如只做剖析、只做缓存优化、只做精度核查），
从对应 Step 进入并在该 Step 收尾，无需走完全部 21 步。